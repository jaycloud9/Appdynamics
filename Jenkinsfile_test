#!/usr/bin/env groovy


  stage('package') {
    node {
        sh 'echo "cloning the GIT repoA"'
        sh 'echo "cloning the GIT repoA"'
	sh 'mkdir /tmp/package1'
    }
  }
  stage('Promote') {
    node {
        sh 'echo "START BUILD PROCESS"'
        sh 'echo "START BUILD PROCESS"'
	sh 'mkdir /tmp/build1'
    }
  }
  stage('Deploy') {
    node {
        sh 'echo "START DEPLOYMENT"'
        sh 'echo "START DEPLOYMENT"'
	sh 'mkdir /tmp/deploy1'
    }
  }
